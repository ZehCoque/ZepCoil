import { ErrorMatcherDirective } from './error-matcher.directive';

describe('ErrorMatcherDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorMatcherDirective();
    expect(directive).toBeTruthy();
  });
});
